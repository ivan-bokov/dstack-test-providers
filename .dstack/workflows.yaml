workflows:
  - name: hello
    provider: 
      repo: https://github.com/ivan-bokov/dstack-test-providers
      name: pytorch-ddp
    image: ubuntu
    commands:
      - printenv
      - mkdir -p output
      - echo 'Hello, world!' > output/hello.txt
    ports: [8888]
    artifacts:
      - output
    resources:
      nodes: 3
      cpu: 1
      memory: 1GB
